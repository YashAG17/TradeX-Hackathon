"""FastAPI dashboard backend that exposes the MEVerse and TradeX runners as REST endpoints.

Wraps logic from `dashboard.py` and `app.py` so the React frontend can drive the
same simulations without coupling to Gradio.
"""
